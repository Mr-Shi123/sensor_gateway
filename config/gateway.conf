# 这是配置文件

# 服务器配置
    # 端口号
    port = 9090
    # 最大连接数量
    max_sensors = 100
    # 心跳时间
    heartbeat_timeout = 60

# 消息队列配置
    # 队列容量
    max_size = 1000
    # 批量处理消息数
    batch_size = 10

# 数据库配置
    db_file = sensor_data.db

# 日志文件配置
    log_file = ./log/gateway.log